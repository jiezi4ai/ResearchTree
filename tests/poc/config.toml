# config for key parameters

[models]
    [models.llm]
        api_key = ""
        model_name = "gemini-2.0-flash"

    [models.embed]
        api_key = ""
        model_name = "models/text-embedding-004"
